{{- if .Values.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "postgres-gateway.name" . }}-users
type: Opaque
stringData:
  userlist.txt: |
    {{- range .Values.pgbouncer.users }}
    "{{ .name }}" "{{ .password }}"
    {{- end }}
{{- end }}
